import Vue from 'vue'
import Vuex from 'vuex'
Vue.use(Vuex)
const actions = {}

const mutations = {}
const state = {
  isLogin: false,
}
const getters = {}
export default new Vuex.Store({
  actions,
  mutations,
  state,
  getters

})